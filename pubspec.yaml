name: rockstar_app
description: 이 시대의 밴드맨이라면.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0

  english_words: ^4.0.0
  provider: ^6.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/rtr_logo.png
  fonts:
    - family: PixelFont
      fonts:
        - asset: assets/fonts/DungGeunMo.ttf